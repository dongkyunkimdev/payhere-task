spring:
  datasource:
    url: ${PAYHERE_DB_CONNECTION_URI:jdbc:mysql://localhost:3306/payhere?serverTimezone=Asia/Seoul&characterEncoding=UTF-8}
    username: root
    password: password

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
        show_sql: true